<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container-xxl d-flex flex-column gap-5">
      <nav class="navbar navbar-light bg-light">
        <div class="container-fluid d-flex justify-content-between flex-wrap">
          <span class="navbar-brand mb-0 h1">Dev Blogs</span>
          <a href="/user/dashboard"><button type="button" class="btn btn-primary">Go back</button></a>
        </div>
      </nav>
      <div class="container-lg d-flex flex-column gap-3">
        <h2>Some recent blogs</h2>
        <div class="container-fluid d-flex gap-3 flex-wrap" id="container">
          <% for( let index = 0; (index < blogs.length); index++ )
          { %>
          
              <div class="card mb-3 flex-grow-1" style="flex-basis: 500px;" onclick="viewBlog(this)" data-id="<%= blogs[index]._id %>">
                <div class="row g-0">
                  <div class="col-md-4">
                    <img
                      src="data:<%= blogs[index].imageMimeType %>;base64,<%= blogs[index].coverimage.toString('base64') %>"
                      class="img-fluid rounded-start"
                      alt="blog image"
                      loading="lazy"
                    />
                  </div>
                  <div class="col-md-8">
                    <div class="card-body">
                      <h5 class="card-title"><%= blogs[index].title %></h5>
                      <h6 class="card-text">
                        <small class="text-muted"
                          >By @<%= blogs[index].authorName %>
                        </small>
                      </h6>
                      <p class="card-text"><%= blogs[index].description %></p>
                      <p class="card-text">
                        <small class="text-muted">
                          createdAt: <%= new
                          Date(blogs[index].createdAt).toLocaleDateString() %>
                          <br />
                          updatedAt: <%= new
                          Date(blogs[index].updatedAt).toLocaleDateString() %>
                        </small>
                      </p>
                    </div>
                  </div>
                </div>
              </div>
          <% } %>
        </div>
      </div>
    </div>
    <script>
        function viewBlog(el){
            location.href = `/user/blogs/viewBlog/${el.dataset.id}`;
        }
    </script>
  </body>
</html>
